// this is a program I do while learning arrays in C language 

#include  <stdio.h>

int main(){
    int myNUm[] = {2,4,5,7,8,86};
    //wanna access it, you can access it through the index numbers of the array
    //unlike other languages it's index number also starts from 0
    printf("%d \n", myNUm[0]);
    printf("%d is the sum of 1st and second element! ", myNUm[0] + myNUm[1]);

    // we can also change the element in a array
}